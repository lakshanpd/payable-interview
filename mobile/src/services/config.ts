// Point this at your Django backend. `localhost` only works from the iOS
// simulator; for a physical device or the Android emulator use your
// machine's LAN IP (Android emulator: 10.0.2.2) — see README "Running the
// mobile app".
export const API_BASE_URL = 'http://localhost:8000/api';
